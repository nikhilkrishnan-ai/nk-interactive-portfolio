# GeoSense AI Memory Bank
> **Hidden forensic state file** — read at session start, update after every successful task.
> Last initialized: **2026-07-04** by GeoSense AI Co-Pilot v2.5

---

## 1. Project Identity

| Field | Value |
|---|---|
| **Repo** | `nk-interactive-portfolio` |
| **Product** | GeoSense — Spatiotemporal Telemetry Anomaly Detection Engine |
| **Lead** | Nikhil Krishnan (NK) — Lead Geospatial Forensic Intelligence Expert |
| **Live HUD** | [nikhilkrishnan.dev](https://nikhilkrishnan.dev/) |
| **GitHub Pages** | Deploys `www/` on push to `main` |
| **Mobile shell** | Capacitor 6 (`webDir: www`, `appId: dev.nikhilkrishnan.geosense`) |

---

## 2. Co-Pilot & Memory Pipeline Rules (`.cursorrules`)

- **Persona**: GeoSense AI Co-Pilot v2.5 — forensic-themed, crisp, loyal to NK; Malayalam slang blended professionally.
- **Memory Pipeline** (added 2026-07-04, uncommitted):
  - Read `.ai_memory_bank.md` **first** at the start of every new prompt.
  - **Append/update** this file at the end of every successful task or code update.
  - Track: fixes, current errors, next steps, architectural changes.
  - On structural system changes, output: *"Euclidean verification complete... Memory Pipeline updated, nte chank."*

---

## 3. Architecture Snapshot

```
[ Android / Termux / Ubuntu Edge ]  →  POST JSON telemetry
              ↓
[ python_gps_webhook.py ]  (functions-framework :8080)
              ↓
[ geosense_engine_core.py ]  Haversine distance + velocity threshold
              ↓
Phase 1 (local)  →  forensic JSON response only
Phase 2 (fabric) →  Azure Event Hub → Microsoft Fabric Eventstream
```

### Infrastructure nodes (production target)
- **GEEKOM Client** (Windows/WSL) — `pipeline.py` sync engine, pulls 76,135 telemetry rows from Azure
- **Ubuntu Server** `192.168.10.4` — Docker, PostGIS, Cockpit `:9090`

### Key source files
| File | Role |
|---|---|
| `geosense_engine_core.py` | Core forensic engine — Haversine, velocity check, Case #250-KM demo |
| `python_gps_webhook.py` | HTTP webhook — per-device baseline + trajectory analysis, optional Event Hub forward |
| `requirements.txt` | `functions-framework`, `azure-eventhub`, `cryptography` |
| `Dockerfile` | Python 3.10-slim container for webhook |
| `server/docker-compose.yml` | Compose service `geosense-webhook` on port 8080 |
| `server/start_phase1.sh` | Phase 1 — venv + forensic-only webhook (no Event Hub) |
| `server/fire_phase2.sh` | Phase 2 — loads `server/.env`, enables Fabric stream |
| `server/telemetry_simulator.py` | Ubuntu edge simulator — `legit` / `spoof_250km` scenarios |
| `server/run_forensic_test.sh` | One-shot test against running webhook |
| `server/.env.example` | Template for Event Hub creds (copy → `server/.env`) |
| `www/` | Static portfolio — `index.html`, `truth_detector.html`, `geosense_forensic_dashboard.html`, case studies |

### Forensic constants (Case #250-KM)
- Origin: `24.4539°N, 54.3773°E` (Abu Dhabi)
- Spoof: `24.1100°N, 52.7300°E` (Ruwais) — ~250 km in 1 second
- Default speed threshold: **1000 km/h** (`GEOSENSE_SPEED_THRESHOLD_KMH`)

---

## 4. Current Status (2026-07-04)

### Git state
- **Branch**: `main` — up to date with `origin/main`
- **Latest commit**: `7ccd742` — *Update Leonode Labs startup branding and enhance local webhook forensic response*

### Uncommitted / untracked work
| Path | State | Notes |
|---|---|---|
| `.cursorrules` | Modified | Memory Pipeline instructions appended |
| `server/` | Untracked | Full Phase 1/2 shell scripts, docker-compose, simulator, `.env.example` |
| `.dockerignore` | Untracked | Excludes `node_modules`, `www`, `.git`, secrets, venv |
| `.ai_memory_bank.md` | **New** | This file — memory pipeline initialized |

### What is working
- Forensic engine (`geosense_engine_core.py`) — standalone Case #250-KM analysis
- Webhook (`python_gps_webhook.py`) — Phase 1 local mode when `EVENT_HUB_CONNECTION_STR` unset
- Static portfolio in `www/` — auto-deploys via GitHub Actions (`deploy.yml`, `static.yml`)
- Docker build path defined (`Dockerfile` + `server/docker-compose.yml`)

### Current errors / blockers
- `server/.env` **not created** — Phase 2 (Azure Event Hub → Fabric) blocked until creds pasted
- `server/` + `.dockerignore` + `.cursorrules` changes **not committed** to git
- Phase 1 webhook **not verified running** on Ubuntu node in this session (scripts exist, runtime unconfirmed)
- `.ai_memory_bank.md` not yet in `.gitignore` — decide whether to track or keep local-only

---

## 5. Phase Operations Quick Reference

### Phase 1 — Local forensic webhook (no Fabric)
```bash
bash server/start_phase1.sh
# Test:
bash server/run_forensic_test.sh
# or:
python3 server/telemetry_simulator.py --scenario spoof_250km
```

### Phase 2 — Fabric stream enabled
```bash
cp server/.env.example server/.env   # paste EVENT_HUB_CONNECTION_STR
bash server/fire_phase2.sh
```

### Docker (Ubuntu node)
```bash
cd server && docker compose up -d --build
```

---

## 6. Next Steps (priority order)

1. **Commit** `server/`, `.dockerignore`, and `.cursorrules` Memory Pipeline update to `main`
2. **Deploy & smoke-test Phase 1** on Ubuntu server `192.168.10.4`:
   - `bash server/start_phase1.sh`
   - `bash server/run_forensic_test.sh` → expect `SPOOFED` on `spoof_250km`
3. **Configure Phase 2**: create `server/.env` from `.env.example` with real Event Hub connection string
4. **Docker path**: validate `docker compose up` on Ubuntu against live port 8080
5. **Memory hygiene**: decide git policy for `.ai_memory_bank.md` (track vs. local-only)

---

## 7. Session Log

### 2026-07-04 — Memory Pipeline Initialization
- **Task**: NK requested `.ai_memory_bank.md` initialization per new `.cursorrules` Memory Pipeline instructions
- **Action**: Full workspace scan completed; memory bank file created with project status, rules, architecture, and next steps
- **Fixed**: N/A (greenfield memory init)
- **Errors**: None blocking
- **Structural change**: Yes — new persistent memory file introduced

---

*Euclidean verification complete. Memory Pipeline online, nte chank.*

### 2026-07-20 — Session Handshake
- **Task**: User greeting / session reactivation
- **Action**: Memory bank reloaded successfully; awaiting next implementation command
- **Fixed**: N/A
- **Errors**: None
